<div class="col-md-12">
  <div class="group">
    <%= link_to("编辑",edit_group_path, :class => "btn btn-primary pull-right") %>
  </div>
  <h2 class="text-center"> <%= @group.title %> </h2>

   <%= @group.description %>
</div>

