<div class="col-md-12">
  <div class="group">
    <%= link_to("写文章", new_group_post_path(@group), :class => "btn btn-danger btn-primary pull-right") %>
    <% if current_user && current_user == @group.user %>
    <%= link_to("编辑",edit_group_path, :class => "btn btn-primary pull-right") %>
    <% end %>
  </div>
  <h2 class="text-center"> <%= @group.title %> </h2>

   <%= @group.description %>
</div>

